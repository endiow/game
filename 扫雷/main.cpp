#include "minefunction.h"

int main()
{
    mine mc;
    for (;is_run (); delay_fps (60))
    {
        if(mc.gamestart())mc.first_backg();
        mc.main_boomnumber();
        mc.main_mousekick();
        if(mc.main_timemark())mc.main_time();
	}
    return 0;
}
